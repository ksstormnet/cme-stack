# GitLab Service Environment Configuration Template
# This file is committed to the repository as a template
# The actual .env file with real secrets is generated and ignored by git

# =================================================================
# DATABASE CONFIGURATION
# =================================================================
MARIADB_HOST=your-mariadb-host
GITLAB_DB_NAME=cme_dev_gitlab
GITLAB_DB_USER=cme_dev_gitlab_user
GITLAB_DB_PASSWORD=GENERATED_PASSWORD_32_CHARS

# =================================================================
# REDIS CONFIGURATION
# =================================================================
REDIS_HOST=your-redis-host
REDIS_PASSWORD=your-redis-password
REDIS_PORT=6379
GITLAB_REDIS_DB=14

# =================================================================
# GITLAB CONFIGURATION
# =================================================================
GITLAB_ROOT_PASSWORD=GENERATED_ROOT_PASSWORD
GITLAB_EXTERNAL_URL=https://dev-gitlab.cme.ksstorm.dev
GITLAB_REGISTRY_URL=https://dev-registry.cme.ksstorm.dev

# =================================================================
# EMAIL CONFIGURATION (Development - uses MailHog)
# =================================================================
GITLAB_EMAIL_FROM=gitlab@cruisemadeeasy.dev
SMTP_HOST=dev-mailhog
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_DOMAIN=cruisemadeeasy.dev

# =================================================================
# DEVELOPMENT SETTINGS
# =================================================================
GITLAB_ENVIRONMENT=development
